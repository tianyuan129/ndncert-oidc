{
  "tasks": {
    "ca": "deno run --allow-env --allow-net --allow-run --allow-read --allow-write --unstable src/ndncert-oidc-only-ca.ts",
    "client": "deno run --allow-env --allow-net --allow-run --allow-read --allow-write --unstable src/ndncert-oidc-only-client.ts"
  },
  "unstable": ["byonm", "net", "fs"],
  "fmt": {
    "lineWidth": 120
  }
}
